#!/bin/sh

set -eu

if [ "$(hostname)" != "tower" ]; then
    echo "The system backup is configured only on tower." >&2
    exit 1
fi

sudo systemctl start borgmatic.service
sudo systemctl --no-pager --full status borgmatic.service
